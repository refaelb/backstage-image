/* eslint-disable max-classes-per-file */

export class Point {
  static type = 'point';
  type = Point.type;
  constructor({
    row,
    column,
    char
  }) {
    this.row = row;
    this.column = column;
    this.char = char;
  }
}
class Position {
  static type = 'position';
  type = Position.type;
  constructor({
    start,
    end
  }) {
    this.start = start;
    this.end = end;
  }
}
export default Position;